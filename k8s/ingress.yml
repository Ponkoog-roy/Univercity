apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: uni-ingress

spec:
  rules:
  - host: uni.local

    http:
      paths:
      - path: /
        pathType: Prefix

        backend:
          service:
            name: uni-service
            port:
              number: 80